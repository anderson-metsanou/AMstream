<?xml version="1.0" encoding="utf-8"?>
<!-- Layout Item Collection : CardView pour afficher un film sauvegardé -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/surface_container_high"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <!-- Affiche du film -->
        <ImageView
            android:id="@+id/item_coll_poster"
            android:layout_width="80dp"
            android:layout_height="120dp"
            android:contentDescription="@string/detail_title"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Titre du film -->
        <TextView
            android:id="@+id/item_coll_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/item_coll_menu"
            app:layout_constraintStart_toEndOf="@id/item_coll_poster"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Statut personnel (Badge) -->
        <TextView
            android:id="@+id/item_coll_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:background="@color/mystical_purple"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/item_coll_poster"
            app:layout_constraintTop_toBottomOf="@id/item_coll_title" />

        <!-- Note personnelle (RatingBar) -->
        <RatingBar
            android:id="@+id/item_coll_rating"
            style="?android:attr/ratingBarStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:numStars="5"
            android:progressTint="@color/elven_gold"
            android:stepSize="0.5"
            app:layout_constraintStart_toEndOf="@id/item_coll_poster"
            app:layout_constraintTop_toBottomOf="@id/item_coll_status" />

        <!-- Date de visionnage -->
        <TextView
            android:id="@+id/item_coll_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="6dp"
            android:textColor="@color/text_hint"
            android:textSize="12sp"
            app:layout_constraintStart_toEndOf="@id/item_coll_poster"
            app:layout_constraintTop_toBottomOf="@id/item_coll_rating" />

        <!-- Bouton Menu Contextuel (Trois points verticaux) -->
        <ImageButton
            android:id="@+id/item_coll_menu"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Options"
            android:src="@android:drawable/ic_menu_moreoverflow"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/text_secondary" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
